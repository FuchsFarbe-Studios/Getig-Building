<Window x:Class="Getig.WinApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Getig Application"
        Height="640"
        Width="840"
        Loaded="Window_Loaded"
        Closing="OnClosing">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="Please enter the language Details below:"
                   FontSize="16" FontWeight="Bold" />

        <StackPanel Grid.Row="1" Margin="10">
            <TextBox Name="TxtLanguageName" Text="{Binding Path=Name}" />
            <Button Content="Add Language" Click="HandleLanguageSubmit"
                    Margin="0,10,0,0" />
        </StackPanel>

        <TextBlock Grid.Row="2" x:Name="txtResult" />
    </Grid>
</Window>